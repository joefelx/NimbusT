import { LoginCard } from "../components/Component";

function auth() {
  return (
    <div className="bg-black min-h-screen h-auto w-full">
      {/* login card */}
      <LoginCard />
    </div>
  );
}

export default auth;
