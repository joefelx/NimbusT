import { LoginCard } from "../../component";

function auth() {
  return (
    <div className="bg-black min-h-screen h-auto w-full">
      {/* login card */}
      <LoginCard />
    </div>
  );
}

export default auth;
